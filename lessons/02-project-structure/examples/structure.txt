HelloForms/
├── HelloForms.csproj      # target framework, UseWindowsForms, package refs
├── Program.cs             # entry point: Main() → Application.Run(new Form1())
├── Form1.cs               # YOUR code — event handlers, logic
├── Form1.Designer.cs      # GENERATED — InitializeComponent() builds the UI
├── Form1.resx             # form resources (icons, images, localized text)
├── bin/                   # compiled output (gitignored)
└── obj/                   # build intermediates (gitignored)

# Form1.cs and Form1.Designer.cs are the SAME partial class:

# --- Form1.cs (you write this) ---
namespace HelloForms;
public partial class Form1 : Form
{
    public Form1()
    {
        InitializeComponent();   // defined in the .Designer.cs half
    }
}

# --- Form1.Designer.cs (designer writes this) ---
namespace HelloForms;
partial class Form1
{
    private void InitializeComponent() { /* control setup */ }
}
