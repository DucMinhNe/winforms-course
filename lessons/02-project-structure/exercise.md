# Exercise — Lesson 02

1. In a fresh project, answer by locating the file:
   - Where do you write a button's click handler? → `Form1.cs`
   - Where does the control layout code live? → `Form1.Designer.cs`
   - Where is the target framework set? → `HelloForms.csproj`
2. In `Form1.Designer.cs`, find `InitializeComponent()`. Add a button in the
   designer (or by code) and watch a new block appear here.
3. Explain in one sentence why `Form1` is declared `partial`.

**Done when:** you can say which half of the partial class you should edit and
which the designer owns.
